import React from "react";

/**
 * Opt out of default root skeleton UI for the pricing page.
 * The pricing page has its own custom introductory splash curtain animation.
 */
export default function Loading() {
    return null;
}
